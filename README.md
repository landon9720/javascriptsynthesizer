# Getting started

`yarn run serv`

# Menu

```
sin(freq, amp)
square(freq, amp, cycle)
saw(freq, amp)
triangle(freq, amp)
sum(inputs)
seq(inputs)
loop(input, count)
gain(input, factor)
delay(input, time)
```
