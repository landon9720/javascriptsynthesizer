{
  "name": "s",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "build": "webpack",
    "test": "jest",
    "render": "node dist/index.js",
    "debug": "node --inspect-brk dist/index.js",
    "play": "play -t raw -r 44100 -b 32 -e floating-point -c 1 tune.raw",
    "view": "sox -t raw -r 44100 -b 32 -e floating-point -c 1 tune.raw -n spectrogram -X 80 -Y 700 -h -r",
    "stat": "sox -t raw -r 44100 -b 32 -e floating-point -c 1 tune.raw -n stat",
    "clean": "rm -rf ./node_modules; rm -rf ./dist; rm -rf ./temp"
  },
  "author": "Landon Kuhn",
  "license": "UNLICENSED",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "lodash": "^4.17.4",
    "parsimmon": "^1.12.0",
    "source-map-support": "^0.5.9"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.0",
    "copy-webpack-plugin": "^4.1.1",
    "jest": "^23.5.0",
    "webpack": "^3.6.0"
  }
}
